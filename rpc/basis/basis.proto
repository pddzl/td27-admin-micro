syntax = "proto3";

package basis;
option go_package="./basis";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Basis {
  rpc Ping(Request) returns(Response);
}
